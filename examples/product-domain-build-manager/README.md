# Product Domain Build Manager Example
Configuration in this directory creates an AWS IAM role for PDBM.

## Usage
To run this example you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```